const MAX_CHARS=90;let shopInitialized=!1,whisperTarget={},mutedTagEnabled={},lastMessageTick={},currentTick=0;const CHAT_COLOR="#cef3ff",COOLDOWN_TICKS=20,msg=(e,t)=>api.sendMessage(e,[{str:t,style:{color:CHAT_COLOR}}]),nameOf=e=>api.getEntityName(e);function initializeShop(){shopInitialized||(shopInitialized=!0,api.configureShopCategory("Chat",{autoSelectCategory:!0,customTitle:"Chat",sortPriority:10}),api.createShopItem("Chat","WhisperSend",{image:"info-circle",sortPriority:0,customTitle:"Whisper",description:"Send a private message to whoever you have selected.",userInput:{type:"text",placeholderText:"Type whisper message here"},buyButtonText:"Send"}),api.createShopItem("Chat","WhisperSelect",{image:"person",sortPriority:1,customTitle:"Choose Player",description:"Choose a player to whisper to.",userInput:{type:"dropdown",dropdownOptions:[]},buyButtonText:"Select"}),api.createShopItem("Chat","Tags",{image:"volume-slash",sortPriority:2,customTitle:"Tags",description:"Toggles on and off the muted tag for any muted players.",userInput:{type:"dropdown",dropdownOptions:["No Tags","Muted"]},buyButtonText:"Apply"}),api.createShopItem("Chat","ChatMessage",{image:"globe",sortPriority:3,userInput:{type:"text",placeholderText:"Type message here"},buyButtonText:"Send",customTitle:"Chat",description:"Type anything to chat! Mostly useful for muted people."}))}function updateDropdown(e){let t=api.getPlayerIds().filter(t=>t!==e).map(nameOf);api.updateShopItemForPlayer(e,"Chat","WhisperSelect",{userInput:{type:"dropdown",dropdownOptions:t}})}function canSend(e){return!lastMessageTick[e]||currentTick-lastMessageTick[e]>=20||(msg(e,"You must wait 1 second before sending a message."),!1)}function recordSend(e){lastMessageTick[e]=currentTick}function onPlayerJoin(e){initializeShop(),mutedTagEnabled[e]=!1,lastMessageTick[e]=0,api.getPlayerIds().forEach(updateDropdown)}function onPlayerLeave(e){delete mutedTagEnabled[e],delete whisperTarget[e],delete lastMessageTick[e];let t=nameOf(e);api.getPlayerIds().forEach(e=>{whisperTarget[e]===t&&(delete whisperTarget[e],msg(e,"Whisper target cleared.")),updateDropdown(e)})}function onPlayerBoughtShopItem(e,t,s,r,a){if("Chat"===t){if("ChatMessage"===s){if(!canSend(e))return;if(!a||a.length>90)return msg(e,"Message must be 90 characters or less.");recordSend(e);let o=mutedTagEnabled[e]?"[Muted] ":"";api.broadcastMessage([{str:o+nameOf(e)+": ",style:{color:CHAT_COLOR}},{str:a}])}if("Tags"===s&&(mutedTagEnabled[e]="Muted"===a,msg(e,mutedTagEnabled[e]?'Enabled "Muted" tag.':'Disabled "Muted" tag.')),"WhisperSelect"===s&&(whisperTarget[e]=a,msg(e,"Selected: "+a)),"WhisperSend"===s){if(!canSend(e))return;if(!a||a.length>90)return msg(e,"Message must be 90 characters or less.");let n=whisperTarget[e];if(!n)return msg(e,"Select somebody to whisper first.");let i=api.getPlayerIds().find(e=>nameOf(e)===n);if(!i)return msg(e,"Player not found.");recordSend(e),api.sendMessage(i,[{str:"[Whisper] "+nameOf(e)+": ",style:{color:CHAT_COLOR}},{str:a}]),api.sendMessage(e,[{str:"[Whisper → "+n+"] ",style:{color:CHAT_COLOR}},{str:a}])}}}function tick(){currentTick++}
